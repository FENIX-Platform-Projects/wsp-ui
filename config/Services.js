/*global define*/
define(function ( ) {

    'use strict';

    return {
        CL_COUNTRY: "SELECT * FROM test",
        CL_YEAR: "SELECT * FROM test",
        CL_INCOMES: "SELECT * FROM test",
        CL_SUBREGION: "SELECT * FROM test",
        CL_DOMAIN: "SELECT * FROM test",
        CL_INDICATORS: "SELECT * FROM test",

        DATA: "SELECT * FROM test"
    };
});
